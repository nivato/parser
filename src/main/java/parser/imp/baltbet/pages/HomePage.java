package parser.imp.baltbet.pages;

import org.openqa.selenium.WebDriver;

public class HomePage extends BaltBetPage<HomePage>{

	public HomePage(WebDriver driver, String url) {
		super(driver, url);
	}
	
}
